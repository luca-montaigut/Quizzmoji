<div class="mt-5">
  <h1 class="text-center"><%= @result %></h1>
  <% if @result == "Faux, retente ta chance !" %>
    <%= link_to "Rééssayer", movie_path(@movie.id), class:"ml-5 btn btn-danger" %>
  <% else %>
    <br>
    <h1 class="text-center"><%= @movie.answer %></h1>
  <% end %>
</div>

<hr>

<div class="text-center mt-5">
  <%= link_to "« Retour à l'accueil", movies_path, class:"btn btn-info" %>
  <%= link_to "Voir un autre film »", movie_path(Movie.all.sample.id), class:"btn btn-success" %>
</div>