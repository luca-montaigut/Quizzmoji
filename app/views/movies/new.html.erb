<%= form_with(url: admin_movies_path, model: @movie, local: true) do |form| %>
  <% if @movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movie.errors.count, "error") %> prohibited this @movie from being saved:</h2>

      <ul>
      <% @movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<br>
  <%= form.label :emojis, "Emojis" %>
  <%= form.text_field :emojis, class:"form-control" %>
<br>
  <%= form.label :answer, "Réponse" %>
  <%= form.text_field :answer, class:"form-control" %>

<br><br>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
